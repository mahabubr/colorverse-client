export enum tagTypes {
  user = "user",
  auth = "auth",
  pallet = "pallet",
  collection = "collection",
}

export const tagTypesList = [
  tagTypes.user,
  tagTypes.auth,
  tagTypes.pallet,
  tagTypes.collection,
];
