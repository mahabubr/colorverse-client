export enum tagTypes {
  user = "user",
  auth = "auth",
  pallet = "pallet",
  collection = "collection",
  comments = "comments",
}

export const tagTypesList = [
  tagTypes.user,
  tagTypes.auth,
  tagTypes.pallet,
  tagTypes.collection,
  tagTypes.comments,
];
